s8.2
re:用于匹配、搜索和操作文本。
.match(): match() 函数用于从字符串的起始位置匹配正则表达式。如果匹配成功，返回一个匹配对象
.serach()  .findall()  .sub()
.：匹配任意单个字符（除了换行符）。
*：匹配前面的字符零次或多次。
+：匹配前面的字符一次或多次。
?：匹配前面的字符零次或一次。
\d：匹配任意数字字符（等价于 [0-9]）。
\w：匹配任意字母、数字或下划线字符（等价于 [a-zA-Z0-9_]）